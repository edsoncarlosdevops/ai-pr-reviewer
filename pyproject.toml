[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
# Prompt-building strings are long natural-language content; line length isn't a
# useful signal there.
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
